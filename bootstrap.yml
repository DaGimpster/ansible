---
# Patch, deploy Ansible service account + ssh keys to net-new hosts, configure sudoers 
- hosts: bootstrap
  user: root

  vars_files:
  - roles/bootstrap/vars/defined_ansible_account.yml

  roles:
  - bootstrap
